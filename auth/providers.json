{
    "api-key": {
        "name": "api-key",
        "type": "api-key",
        "disabled": true
    },
    "custom-token": {
        "name": "custom-token",
        "type": "custom-token",
        "config": {
            "audience": [
                "users",
                "admin"
            ],
            "requireAnyAudience": false,
            "signingAlgorithm": "HS256",
            "useJWKURI": false
        },
        "secret_config": {
            "signingKeys": [
                "mr-row-key"
            ]
        },
        "disabled": false,
        "metadata_fields": [
            {
                "required": true,
                "name": "userId",
                "field_name": "userId"
            }
        ]
    }
}
